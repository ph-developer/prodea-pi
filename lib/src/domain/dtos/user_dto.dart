import '../entities/user.dart';

extension UserDTO on User {
  User copyWith({
    String? id,
    String? email,
  }) {
    return User(
      id: id ?? this.id,
      email: email ?? this.email,
    );
  }
}
